name: Nightly Quality

on:
  schedule:
    - cron: "0 6 * * 0"
  workflow_dispatch:

concurrency:
  group: nightly-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  stress-and-property:
    name: Nightly stress/property suite
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Run property and stress tests
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci-artifacts
          cargo test -p tau-coding-agent property_ -- --nocapture | tee ci-artifacts/property-tests.log
          cargo test -p tau-coding-agent stress_parallel_appends_high_volume_remain_consistent -- --nocapture | tee ci-artifacts/stress-tests.log

      - name: Upload nightly quality logs
        uses: actions/upload-artifact@v6
        with:
          name: nightly-quality-logs
          path: |
            ci-artifacts/property-tests.log
            ci-artifacts/stress-tests.log

  benchmark-tau-tui:
    name: Nightly benchmark capture (tau-tui)
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Run tau-tui benchmarks
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci-artifacts
          cargo bench -p tau-tui --bench render_bench -- --noplot | tee ci-artifacts/tau-tui-bench.log
          cat > ci-artifacts/benchmark-metadata.json <<JSON
          {
            "workflow": "nightly-quality",
            "suite": "tau-tui/render_bench",
            "sha": "${GITHUB_SHA}",
            "run_id": "${GITHUB_RUN_ID}",
            "run_number": "${GITHUB_RUN_NUMBER}"
          }
          JSON

      - name: Upload benchmark artifacts
        uses: actions/upload-artifact@v6
        with:
          name: nightly-tau-tui-benchmarks
          if-no-files-found: warn
          path: |
            ci-artifacts/tau-tui-bench.log
            ci-artifacts/benchmark-metadata.json
            target/criterion
