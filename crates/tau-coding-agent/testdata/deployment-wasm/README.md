# Deployment WASM Testdata

This directory contains deterministic WASM fixture assets used by packaging/runtime tests.

- `edge-runtime.wasm`: minimal valid WASM binary header fixture (`\0asm` magic + version bytes).
