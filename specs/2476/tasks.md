# Tasks #2476 - implement startup template builtin fallback and source diagnostics

1. T1 (RED): add C-01..C-03 conformance tests and capture failing `spec_2476` run.
2. T2 (GREEN): implement built-in template fallback + template diagnostics.
3. T3 (REFACTOR): keep fallback logic deterministic and isolated.
4. T4 (VERIFY): run scoped fmt/clippy/tests/mutation and capture evidence.
