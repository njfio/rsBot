# Tasks #2556

1. [x] T1 (tests): add RED conformance/regression coverage for default-local policy config and startup diagnostics.
2. [x] T2 (impl): set `ToolPolicy` default memory embedding provider to local and keep explicit remote override behavior.
3. [x] T3 (impl): constrain remote API fallback wiring to remote providers only.
4. [x] T4 (docs): update memory operations runbook for default-local + explicit-remote configuration.
5. [x] T5 (verify): run fmt/clippy/scoped tests, mutation in diff, live validation, and workspace gate.
