# Milestone M31: Split Module Documentation Wave 4

Status: Draft

## Objective

Advance split-module rustdoc coverage for additional RPC dispatch/parsing and
GitHub issue helper modules while extending guard assertions.

## Scope

In scope:

- rustdoc additions for selected wave-4 split modules
- guard script expansion with wave-4 marker assertions
- scoped compile/test verification for touched crates

Out of scope:

- broad repository-wide documentation parity in one milestone
- behavior changes unrelated to documentation baseline

## Success Signals

- M31 hierarchy exists and is active with epic/story/task/subtask linkage.
- wave-4 helper modules gain rustdoc marker coverage.
- split-module guard and scoped crate validations remain green.

## Issue Hierarchy

Milestone: GitHub milestone `M31 Split Module Documentation Wave 4`

Epic:

- `#2128` Epic: M31 Split-Module Rustdoc Coverage Wave 4

Story:

- `#2129` Story: M31.1 Document RPC dispatch/parsing + issue runtime split helpers

Task:

- `#2130` Task: M31.1.1 Add rustdoc to wave-4 split helpers and extend guard

Subtask:

- `#2131` Subtask: M31.1.1a Document RPC dispatch/parsing + issue error helper modules
