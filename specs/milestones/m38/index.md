# Milestone M38: Split Module Documentation Wave 11

Status: Draft

## Objective

Advance split-module rustdoc coverage for `tau-provider` auth runtime helper
modules while extending guard assertions.

## Scope

In scope:

- rustdoc additions for selected wave-11 provider auth runtime modules
- guard script expansion with wave-11 marker assertions
- scoped compile/test verification for touched crate

Out of scope:

- broad repository-wide documentation parity in one milestone
- behavior changes unrelated to documentation baseline

## Success Signals

- M38 hierarchy exists and is active with epic/story/task/subtask linkage.
- wave-11 provider auth runtime modules gain rustdoc marker coverage.
- split-module guard and scoped crate validations remain green.

## Issue Hierarchy

Milestone: GitHub milestone `M38 Split Module Documentation Wave 11`

Epic:

- `#2184` Epic: M38 Split-Module Rustdoc Coverage Wave 11

Story:

- `#2185` Story: M38.1 Document provider auth runtime split helpers

Task:

- `#2186` Task: M38.1.1 Add rustdoc to provider auth runtime split modules and extend guard

Subtask:

- `#2187` Subtask: M38.1.1a Document provider auth backend and shared core modules
