# Milestone M30: Split Module Documentation Wave 3

Status: Draft

## Objective

Advance split-module rustdoc coverage across remaining RPC and GitHub helper
modules while extending regression marker guardrails.

## Scope

In scope:

- rustdoc additions for selected RPC and GitHub helper split modules
- guard script expansion with wave-3 marker assertions
- scoped compile/test verification for touched crates

Out of scope:

- full repository-wide documentation parity in one milestone
- behavior changes unrelated to documentation baseline

## Success Signals

- M30 hierarchy exists and is active with epic/story/task/subtask linkage.
- wave-3 helper modules gain rustdoc marker coverage.
- split-module guard and scoped crate validations remain green.

## Issue Hierarchy

Milestone: GitHub milestone `M30 Split Module Documentation Wave 3`

Epic:

- `#2120` Epic: M30 Split-Module Rustdoc Coverage Wave 3

Story:

- `#2121` Story: M30.1 Document RPC + GitHub helper split modules

Task:

- `#2122` Task: M30.1.1 Add rustdoc to RPC/GitHub split helpers and extend guard

Subtask:

- `#2123` Subtask: M30.1.1a Document RPC + issue helper modules
