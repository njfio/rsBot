# Milestone M32: Split Module Documentation Wave 5

Status: Draft

## Objective

Advance split-module rustdoc coverage for startup helper/runtime modules while
extending guard assertions.

## Scope

In scope:

- rustdoc additions for selected wave-5 startup modules
- guard script expansion with wave-5 marker assertions
- scoped compile/test verification for touched crates

Out of scope:

- broad repository-wide documentation parity in one milestone
- behavior changes unrelated to documentation baseline

## Success Signals

- M32 hierarchy exists and is active with epic/story/task/subtask linkage.
- wave-5 startup modules gain rustdoc marker coverage.
- split-module guard and scoped crate validations remain green.

## Issue Hierarchy

Milestone: GitHub milestone `M32 Split Module Documentation Wave 5`

Epic:

- `#2136` Epic: M32 Split-Module Rustdoc Coverage Wave 5

Story:

- `#2137` Story: M32.1 Document startup split helper modules

Task:

- `#2138` Task: M32.1.1 Add rustdoc to startup split helpers and extend guard

Subtask:

- `#2139` Subtask: M32.1.1a Document startup model/multi-channel helper modules
