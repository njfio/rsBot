# Tasks #2063

Status: Completed
Spec: specs/2063/spec.md
Plan: specs/2063/plan.md

## Ordered Tasks

- T1: Add split threshold/marker guardrail checks for `tools.rs` (RED).
- T2: Extract a high-volume tools runtime domain module from `tools.rs`.
- T3: Run tau-tools + consuming runtime integration tests and guardrails
  (GREEN).
- T4: Post closure evidence and close `#2063` + roll-up into `#2042`.
