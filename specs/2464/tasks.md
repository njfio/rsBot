# Tasks #2464 - runtime heartbeat policy hot-reload without restart

1. T1: define conformance + regression expectations in #2465 spec/tests.
2. T2: implement heartbeat policy reload path and deterministic diagnostics.
3. T3: run scoped verification (`fmt`, `clippy`, `tau-runtime` tests) and close evidence.
