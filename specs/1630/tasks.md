# Issue 1630 Tasks

Status: Implemented

## Ordered Tasks

T1 (tests-first): add `scripts/dev/test-contract-runner-remnants.sh` with docs-matrix expectation; run RED.

T2: update `docs/guides/transports.md` with explicit removed contract-runner migration matrix.

T3: run GREEN harness plus targeted removed-runner validation tests in `tau-onboarding` and `tau-coding-agent`.

T4: run scoped roadmap/fmt/clippy checks and prepare PR evidence mapping AC -> tests.

## Tier Mapping

- Functional: inventory + docs/demo conformance harness
- Integration: targeted removed-runner CLI validation tests
- Regression: dispatch wiring absence checks
