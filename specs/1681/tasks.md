# Issue 1681 Tasks

Status: In Progress

## Ordered Tasks

T1 (tests-first): capture RED by executing missing package-manifest split
harness.

T2: extract schema/validation/IO helpers into
`crates/tau-skills/src/package_manifest/`.

T3: add split-conformance harness and run GREEN harness + targeted tests.

T4: run roadmap/fmt/clippy checks and prepare PR closure evidence.

## Tier Mapping

- Functional: split conformance harness (line budget + module/file markers)
- Regression: targeted package-manifest tests
- Integration: issue verification command sequence
