# Issue 1640 Tasks

Status: Implemented

## Ordered Tasks

T1 (tests-first): add `scripts/dev/test-startup-safety-policy-precedence.sh` and capture RED.

T2: update `docs/guides/startup-di-pipeline.md` with explicit safety-policy precedence section.

T3: run GREEN harness and targeted tau-startup precedence tests.

T4: run scoped roadmap/fmt/clippy checks and prepare PR evidence.

## Tier Mapping

- Functional: source/docs precedence contract harness
- Regression: targeted tau-startup precedence tests
- Integration: harness + scoped quality checks
